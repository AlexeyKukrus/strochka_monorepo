{
  "name": "strochka-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "pnpm install",
    "dev": "pnpm run build && nest start --watch",
    "test": "pnpm -r run test",
    "lint": "eslint './client/src/**/*.ts' './server/src/**/*.ts' --fix",
    "format": "pnpm -r run format",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start",
    "deploy": "echo 'Deploy script here'"
  },
  "dependencies": {
    "pnpm": "^9.15.1"
  }
}
